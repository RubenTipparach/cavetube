import React from 'react';
import ReactDOM from 'react-dom';

// A device agnostic web view of the current video being played, 
// no controls will be provided, and windoe will default to full screen.
export default class TubeViewer extends React.Component {
    render() {
        return (<div>This is Library</div>);
    }
}